---
phase: 03-multi-lang
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/core/engine/retrieval/chunker/languages.ts, test/multi-lang-load.test.ts]
autonomous: true
requirements: [LANG-01]

must_haves:
  truths:
    - "Grammars for Python, Go, and Rust are listed as optional dependencies"
    - "All three grammars can be loaded dynamically via LanguageLoaders"
    - "Test suite confirms grammars are available on the current platform"
  artifacts:
    - path: "package.json"
      provides: "Optional dependencies for tree-sitter grammars"
    - path: "test/multi-lang-load.test.ts"
      provides: "Grammar loading verification"
  key_links:
    - from: "src/core/engine/retrieval/chunker/languages.ts"
      to: "node_modules/tree-sitter-python"
      via: "dynamic import"
---

<objective>
Set up the multi-language foundation by installing Tree-sitter grammars for Python, Go, and Rust and verifying their accessibility within the Synapse runtime.

Purpose: Enable Synapse to parse polyglot repositories beyond TypeScript.
Output: Updated dependencies and verified grammar loaders.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/M001-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-3/03-RESEARCH.md
@package.json
@src/core/engine/retrieval/chunker/languages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add polyglot grammars to optionalDependencies</name>
  <files>package.json</files>
  <action>
    Add the following to `optionalDependencies` in `package.json`:
    - "tree-sitter-python": "0.25.0"
    - "tree-sitter-go": "0.25.0"
    - "tree-sitter-rust": "0.24.0"
    
    Then run `npm install` to update the lockfile. 
    Note: These are optional to ensure Synapse remains functional if native builds fail on niche platforms, but they are required for Phase 3 functionality.
  </action>
  <verify>
    <automated>grep -E "tree-sitter-(python|go|rust)" package.json</automated>
  </verify>
  <done>package.json updated and dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Update LanguageLoaders for missing grammars</name>
  <files>src/core/engine/retrieval/chunker/languages.ts</files>
  <action>
    Ensure `LANGUAGE_LOADERS` and `LANGUAGE_PACKAGES` in `src/core/engine/retrieval/chunker/languages.ts` are fully populated for `python`, `go`, and `rust`. 
    
    Verify that the `python` and `go` loaders use `mod.default || mod` and `rust` uses `mod.default || mod` (per RESEARCH.md patterns).
    
    The file already has some entries; ensure they match the latest research recommendations for optional dependency loading.
  </action>
  <verify>
    <automated>tsx --check src/core/engine/retrieval/chunker/languages.ts</automated>
  </verify>
  <done>Language configuration updated and type-safe</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify grammar loading</name>
  <files>test/multi-lang-load.test.ts</files>
  <behavior>
    - Load the `AstChunker` service (or `LANGUAGE_LOADERS` directly).
    - For each language (python, go, rust):
      - Invoke the loader.
      - Verify the result is a valid Tree-sitter language object (has `name` or can be set on a parser).
  </behavior>
  <action>
    Create a test that iterates through Python, Go, and Rust and attempts to load their grammars. Use the native `node --test` runner.
  </action>
  <verify>
    <automated>tsx --test test/multi-lang-load.test.ts</automated>
  </verify>
  <done>All three grammars load successfully in the test environment</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime → Native Modules | Dynamic loading of native binaries from node_modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | package.json | mitigate | Use specific versions for grammars to prevent supply chain injection |
| T-03-01-02 | D | runtime | mitigate | Treat grammars as optionalDependencies so failures don't crash core Synapse |
</threat_model>

<verification>
Run `npm test test/multi-lang-load.test.ts` to confirm grammars are reachable.
</verification>

<success_criteria>
1. Grammars installed.
2. Loaders updated.
3. Verification test passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-lang/03-01-SUMMARY.md`
</output>
