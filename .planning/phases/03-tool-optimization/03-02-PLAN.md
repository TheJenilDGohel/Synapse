---
phase: 03-tool-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interfaces/mcp/tools/graph-tools.ts
  - src/interfaces/mcp/tools/kg-delete-tools.ts
  - src/interfaces/mcp/tools/retrieval.ts
  - src/interfaces/mcp/tools/memory-store.ts
  - src/interfaces/mcp/common/normalizers/retrieval.ts
  - src/interfaces/mcp/common/normalizers/memory.ts
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03]
must_haves:
  truths:
    - "Knowledge Graph tools are bundled into manageable controllers"
    - "Tool outputs are standardized to a single items[] array"
    - "Redundant legacy arrays are removed from default outputs"
  artifacts:
    - path: "src/interfaces/mcp/tools/graph-tools.ts"
      provides: "Bundled KG controllers"
    - path: "src/interfaces/mcp/common/normalizers/retrieval.ts"
      provides: "Optimized retrieval normalizers"
    - path: "src/interfaces/mcp/common/normalizers/memory.ts"
      provides: "Optimized memory normalizers"
  key_links:
    - from: "src/interfaces/mcp/tools/graph-tools.ts"
      to: "memory-service"
      via: "discriminated union actions"
    - from: "src/interfaces/mcp/tools/retrieval.ts"
      to: "normalizeSearchHybridResult"
      via: "standardized items array"

---

<objective>
Reduce the context window consumption of MCP tools by bundling Knowledge Graph operations into controllers and eliminating redundant output arrays.

Purpose: Minimize the "context tax" of tool definitions and response payloads.
Output: Refactored KG tools and optimized normalization logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tool-optimization/01-RESEARCH.md
@src/interfaces/mcp/tools/graph-tools.ts
@src/interfaces/mcp/tools/kg-delete-tools.ts
@src/interfaces/mcp/common/normalizers/retrieval.ts
@src/interfaces/mcp/common/normalizers/memory.ts
@src/interfaces/mcp/common/terse-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Knowledge Graph Controllers</name>
  <files>src/interfaces/mcp/tools/graph-tools.ts, src/interfaces/mcp/tools/kg-delete-tools.ts</files>
  <action>
    Refactor existing KG tools into two primary controllers:
    - `synapse_kg_manage`: Bundle all mutation actions (add_entity, add_triple, add_entity_batch, add_triple_batch, invalidate, delete_entity, delete_entities_batch, delete_triples_batch, manage_branches, ingest_markdown, ingest_json).
    - `synapse_kg_query`: Bundle all read actions (list_entities, query, as_of, timeline, stats, nest_list, nest_branches, nest_tree, traverse, bridges, diary_read, check_duplicate).
    
    Implementation details:
    1. Use `z.discriminatedUnion('action', [...])` for the `inputSchema`.
    2. Move logic from `kg-delete-tools.ts` into `graph-tools.ts`.
    3. Update `registerGraphTools` to register only these two controllers.
    4. Ensure the `action` parameter correctly routes to the corresponding `memory` service method.
    5. Maintain `terse` support for mutations per existing patterns.
  </action>
  <verify>
    <automated>npm run build && node bin/synapse-doctor.js mcp --tools | grep -E "synapse_kg_manage|synapse_kg_query"</automated>
  </verify>
  <done>KG tools consolidated into two controllers; individual tool tokens reduced.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize Retrieval and Memory Tool Outputs</name>
  <files>src/interfaces/mcp/common/normalizers/retrieval.ts, src/interfaces/mcp/common/normalizers/memory.ts, src/interfaces/mcp/tools/retrieval.ts, src/interfaces/mcp/tools/memory-store.ts</files>
  <action>
    Eliminate redundant arrays in tool outputs by standardizing on `items[]`:
    1. Update `normalizeSymbolResult` (retrieval.ts): Stop returning `definitions` and `exports` by default. They should only be included if `include_legacy_arrays` is true.
    2. Update `normalizeSearchHybridResult` (retrieval.ts): Ensure `results` is only included if requested.
    3. Update `normalizeMemorySuggestionResult` (memory.ts): Remove `suggestions` from default output, use `items` only.
    4. Update `normalizeUsageResult`, `normalizeCallersResult`, `normalizeDefinitionResult`, `normalizeImplementationsResult`, `normalizeRenamePreviewResult`, and `normalizeRelatedMemoriesResult` to respect `includeLegacyArrays` and default to NOT including redundant arrays.
    5. Update the tool definitions in `src/interfaces/mcp/tools/retrieval.ts` and `memory-store.ts` to ensure `include_legacy_arrays` defaults to `false`.
  </action>
  <verify>
    <automated>npm test src/interfaces/mcp/common/normalizers/retrieval.test.ts</automated>
  </verify>
  <done>Redundant arrays removed from tool outputs; response size reduced by ~40-50% for list operations.</done>
</task>

<task type="auto">
  <name>Task 3: Final Cleanup and Schema Validation</name>
  <files>src/interfaces/mcp/tools/index.ts, src/interfaces/mcp/tools/kg-delete-tools.ts</files>
  <action>
    1. Remove `kg-delete-tools.ts` after migrating its logic to `graph-tools.ts`.
    2. Update `src/interfaces/mcp/tools/index.ts` to remove the reference to `kg-delete-tools.ts`.
    3. Run a full build to ensure all Zod schemas in controllers are valid and correctly typed.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Project structure cleaned; KG delete tools removed; full build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → MCP Controller | The `action` parameter must be strictly validated against the enum to prevent calling unauthorized internal methods. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `synapse_kg_manage` | mitigate | Use Zod discriminated union to enforce valid actions and payloads. |
| T-03-02 | Denial of Service | Tool Output | mitigate | Standardize on pagination (items[] + limit/offset) to prevent massive payloads from crashing the client. |
</threat_model>

<verification>
1. Verify `synapse_kg_manage` and `synapse_kg_query` appear in `list_tools`.
2. Verify `synapse_get_symbol` returns only `items` (not `definitions`) when `include_legacy_arrays=false`.
3. Verify `synapse_memory_suggest_relations` returns only `items` (not `suggestions`).
</verification>

<success_criteria>
- Total MCP tool count reduced by at least 20 entries.
- Redundant arrays removed from all search/suggestion tool outputs.
- All KG operations remain fully functional through bundled controllers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tool-optimization/03-02-SUMMARY.md`
</output>
