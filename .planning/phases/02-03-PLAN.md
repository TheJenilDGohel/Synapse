---
phase: "02-react-docs"
plan: "03"
type: "execute"
wave: 1
depends_on: ["02-02"]
files_modified: [
  "docs/src/css/custom.css",
  "docs/docusaurus.config.ts",
  "docs/sidebars.ts"
]
autonomous: true
requirements: ["RE-03"]
must_haves:
  truths:
    - "Site theme uses professional Indigo/Violet colors instead of default Docusaurus green"
    - "Social card and logo configurations are present in docusaurus.config.ts"
    - "Footer contains links to GitHub, Documentation, and Blog"
    - "'npm run docs:build' completes successfully from root"
  artifacts:
    - path: "docs/src/css/custom.css"
      provides: "Synapse brand styling"
    - path: "docs/docusaurus.config.ts"
      provides: "Site metadata and navigation config"
  key_links:
    - from: "package.json"
      to: "docs/package.json"
      via: "npm run docs:build"
---

<objective>
Finalize the Docusaurus site with Synapse branding and ensure a successful production build.

Purpose: Professionalize the documentation site and verify it's ready for deployment.
Output: Branded, buildable Docusaurus site.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/M002-ROADMAP.md
@docs/docusaurus.config.ts
@docs/src/css/custom.css
@docs/sidebars.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Professional Palette Integration</name>
  <files>docs/src/css/custom.css</files>
  <action>
    Update the CSS variables in `docs/src/css/custom.css` to use a professional "Synapse Indigo" palette. 
    Replace default green values (#2e8555, #25c2a0) with Indigo equivalents:
    
    Light Mode:
    - --ifm-color-primary: #6366f1 (Indigo 500)
    - --ifm-color-primary-dark: #4f46e5
    - --ifm-color-primary-darker: #4338ca
    - --ifm-color-primary-darkest: #3730a3
    - --ifm-color-primary-light: #818cf8
    - --ifm-color-primary-lighter: #a5b4fc
    - --ifm-color-primary-lightest: #c7d2fe

    Dark Mode:
    - --ifm-color-primary: #818cf8 (Indigo 400)
    - --ifm-color-primary-dark: #6366f1
    - --ifm-color-primary-darker: #4f46e5
    - --ifm-color-primary-darkest: #4338ca
    - --ifm-color-primary-light: #a5b4fc
    - --ifm-color-primary-lighter: #c7d2fe
    - --ifm-color-primary-lightest: #e0e7ff
  </action>
  <verify>
    <automated>grep "#6366f1" docs/src/css/custom.css</automated>
  </verify>
  <done>Custom CSS reflects the Indigo brand palette.</done>
</task>

<task type="auto">
  <name>Task 2: Identity & Metadata Polish</name>
  <files>docs/docusaurus.config.ts</files>
  <action>
    Refine site identity in `docs/docusaurus.config.ts`:
    1. Update `themeConfig.image` to use a placeholder social card if necessary (or keep current if fine).
    2. Ensure `themeConfig.navbar.logo` alt text and src are correct.
    3. Update `themeConfig.footer` links to be complete and accurate:
       - Link 'Introduction' to '/docs/index'
       - Link 'Architecture' to '/docs/architecture'
       - Ensure GitHub link points to 'https://github.com/TheJenilDGohel/synapse'
    4. Verify `title` and `tagline` reflect Synapse's core value (e.g., "Synapse: The Transmission Layer for AI Context").
  </action>
  <verify>
    <automated>grep "Synapse" docs/docusaurus.config.ts</automated>
  </verify>
  <done>Site metadata and navigation are refined and accurate.</done>
</task>

<task type="auto">
  <name>Task 3: Production Build Verification</name>
  <files>docs/package.json</files>
  <action>
    Run the production build from the root directory to ensure all MDX, components, and configurations are valid for production deployment.
    Command: `npm run docs:build`
  </action>
  <verify>
    <automated>npm run docs:build</automated>
  </verify>
  <done>'build' directory exists inside 'docs/' with a successful Docusaurus build output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> Docusaurus | Production build must be free of insecure links or malicious scripts |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | docusaurus.config.ts | mitigate | Use 'onBrokenLinks: throw' to prevent dead/malicious link ingestion during build |
</threat_model>

<verification>
- Run `npm run docs:build` and ensure exit code 0.
- Check `docs/build/index.html` existence.
</verification>

<success_criteria>
- Custom palette applied to CSS.
- Navigation links functional in config.
- Production build succeeds without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-docs/02-03-SUMMARY.md`
</output>
