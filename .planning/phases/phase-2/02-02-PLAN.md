---
phase: "02-local-enrichment"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/core/engine/enrichment/worker.ts
  - src/services/enrichment-service.ts
  - src/core/engine/retrieval/sqlite-vec/indexer.ts
autonomous: true
requirements: [ENR-01, ENR-02]
---

<objective>
Implement a background enrichment worker to process code chunks using the LLM runtime without blocking the main MCP server loop. Orchestrate the enrichment flow post-indexing.

Purpose: Maintain server responsiveness while performing heavy LLM indexing.
Output: EnrichmentWorker and EnrichmentService.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-2/02-01-SUMMARY.md
@src/core/engine/retrieval/sqlite-vec/indexer.ts
@src/core/engine/retrieval/chunker/service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Enrichment Worker Thread</name>
  <files>src/core/engine/enrichment/worker.ts</files>
  <action>
    Create src/core/engine/enrichment/worker.ts using Node.js 'worker_threads'.
    The worker should initialize the LlmRuntime from Plan 01.
    Implement a message handler that receives a code chunk, runs the 'enrichment' prompt (Summary + Tags), and returns the results.
    Include error handling for worker crashes or OOM.
  </action>
  <verify>
    <automated>tsx test/enrichment-worker.test.ts</automated>
  </verify>
  <done>Worker thread successfully performs inference and returns metadata to the parent thread.</done>
</task>

<task type="auto">
  <name>Task 2: Implement EnrichmentService and Integration</name>
  <files>src/services/enrichment-service.ts, src/core/engine/retrieval/sqlite-vec/indexer.ts</files>
  <action>
    Create src/services/enrichment-service.ts to manage the background processing queue.
    Implement logic to track which chunks need enrichment (initially using a simple set or DB query).
    Modify src/core/engine/retrieval/sqlite-vec/indexer.ts to 'notify' the EnrichmentService when new chunks are indexed.
    Ensure the service respects a concurrency limit for background workers.
  </action>
  <verify>
    Verify that indexing a file triggers a background enrichment task without increasing the indexing latency significantly.
  </verify>
  <done>Enrichment service is active and responding to new chunk events.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main Thread → Worker Thread | Passing raw code chunks to worker for processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Denial of Service | worker.ts | mitigate | Implement resource monitoring and auto-restart for zombie workers |
| T-02-02-02 | Information Disclosure | worker.ts | mitigate | Ensure worker environment is isolated and has no network access (enforced by process config) |
</threat_model>

<success_criteria>
- Enrichment processing runs in a separate thread.
- Main MCP server remains responsive during background enrichment.
- Chunks are successfully enriched with summaries and tags post-indexing.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-2/02-02-SUMMARY.md`
</output>
