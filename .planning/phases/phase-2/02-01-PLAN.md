---
phase: "02-local-enrichment"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/engine/enrichment/runtime.ts
  - src/core/engine/enrichment/model.ts
  - test/enrichment-runtime.test.ts
autonomous: true
requirements: [ENR-01]
user_setup:
  - service: "Local LLM"
    why: "Background code enrichment requires Qwen2.5-Coder-1.5B model (~3GB disk space)"
    dashboard_config:
      - task: "Ensure sufficient disk space in ~/.cache/huggingface"
        location: "System Disk"

must_haves:
  truths:
    - "System can detect WebGPU or WASM fallback for Transformers.js v4"
    - "Qwen2.5-Coder-1.5B model can be initialized without crashing"
    - "LlmRuntime generates text response for a given code chunk prompt"
  artifacts:
    - path: "src/core/engine/enrichment/runtime.ts"
      provides: "Transformers.js v4 integration"
    - path: "src/core/engine/enrichment/model.ts"
      provides: "Model management and WebGPU configuration"
  key_links:
    - from: "src/core/engine/enrichment/runtime.ts"
      to: "@huggingface/transformers"
      via: "dynamic import"
---

<objective>
Implement the core LLM runtime using Transformers.js v4 to enable local, privacy-preserving code enrichment. This wave focuses on model loading and basic inference capability.

Purpose: Provide a reliable execution environment for the Qwen2.5-Coder model.
Output: LlmRuntime and EnrichmentModelService.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M001-ROADMAP.md
@.planning/REQUIREMENTS.md
@src/core/engine/retrieval/embedding/service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement LlmRuntime with Transformers.js v4</name>
  <files>src/core/engine/enrichment/runtime.ts, test/enrichment-runtime.test.ts</files>
  <behavior>
    - When initialized, it should detect WebGPU support and configure the Transformers.js environment.
    - generate(prompt, options) should return a string using the loaded model.
    - Should handle timeouts and resource cleanup.
  </behavior>
  <action>
    Create src/core/engine/enrichment/runtime.ts. 
    Use dynamic import for '@huggingface/transformers' (v4). 
    Configure 'env.allowLocalModels = true' and 'env.useWebGPU = true' (if supported).
    Implement LlmRuntime class with a 'generate' method that uses the 'text-generation' pipeline.
    Refer to src/core/engine/retrieval/embedding/service.ts for the pipeline loading pattern.
  </action>
  <verify>
    <automated>tsx --test test/enrichment-runtime.test.ts</automated>
  </verify>
  <done>LlmRuntime class implemented and verified with a basic prompt/response test.</done>
</task>

<task type="auto">
  <name>Task 2: Implement EnrichmentModelService for Qwen2.5-Coder-1.5B</name>
  <files>src/core/engine/enrichment/model.ts</files>
  <action>
    Create src/core/engine/enrichment/model.ts.
    Define EnrichmentModelService to manage model IDs (defaulting to 'Qwen/Qwen2.5-Coder-1.5B-Instruct').
    Implement logic to handle model caching and initialization status.
    Ensure it supports the same cache directory configuration as the EmbeddingService.
  </action>
  <verify>
    Check that model ID is correctly configured and service initializes in a test environment (using a tiny mock model if needed for CI).
  </verify>
  <done>Model management service implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime → Model Cache | Loading untrusted model weights from local cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | model.ts | mitigate | Use official HuggingFace revision hashes for model loading |
| T-02-01-02 | Denial of Service | runtime.ts | mitigate | Implement strict inference timeouts and memory limits for WebGPU |
</threat_model>

<success_criteria>
- LlmRuntime successfully loads and runs a test generation.
- Model loading respects user-configured cache directories.
- Transformers.js v4 is used with WebGPU acceleration where available.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-2/02-01-SUMMARY.md`
</output>
