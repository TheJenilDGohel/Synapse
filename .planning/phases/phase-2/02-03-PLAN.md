---
phase: "02-local-enrichment"
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/core/engine/retrieval/sqlite-vec/schema.ts
  - src/core/engine/retrieval/sqlite-vec/service.ts
  - src/core/engine/retrieval/search/hybrid-ranking.ts
autonomous: true
requirements: [ENR-02, UX-01]
---

<objective>
Persist AI-generated metadata for code chunks and refine the hybrid search logic to prioritize enriched content. This completes the loop by improving retrieval relevance for "conceptual" queries.

Purpose: Bridge the gap between raw code and semantic intent using enriched metadata.
Output: Updated DB schema and weighted hybrid search logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-2/02-02-SUMMARY.md
@src/core/engine/retrieval/sqlite-vec/schema.ts
@src/core/engine/retrieval/search/hybrid-ranking.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema and Implement Persistence</name>
  <files>src/core/engine/retrieval/sqlite-vec/schema.ts, src/core/engine/retrieval/sqlite-vec/service.ts</files>
  <action>
    Increment SCHEMA_VERSION in src/core/engine/retrieval/sqlite-vec/schema.ts.
    Add columns 'enriched_summary' (TEXT) and 'enriched_tags' (TEXT) to the 'chunks' table in the migration logic.
    Update SqliteVecIndexService to provide a method for updating these columns for a given chunk ID.
    Ensure EnrichmentService (from Plan 02) calls this method upon successful enrichment.
  </action>
  <verify>
    Verify that the 'chunks' table has the new columns and that data can be persisted and retrieved.
  </verify>
  <done>Database schema updated and persistence for metadata is working.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Metadata into Hybrid Search Fusion</name>
  <files>src/core/engine/retrieval/search/hybrid-ranking.ts, test/hybrid-search-enrichment.test.ts</files>
  <behavior>
    - Hybrid search should boost results where the query matches terms in 'enriched_summary' or 'enriched_tags'.
    - "Conceptual" queries (e.g., "where is the retry logic?") should rank enriched chunks higher than raw lexical matches if metadata confirms the intent.
  </behavior>
  <action>
    Modify fuseRankAndRerank in src/core/engine/retrieval/search/hybrid-ranking.ts.
    Introduce a 'metadata_score' component to the FusedResult.
    Implement a simple text-matching boost for enriched metadata fields.
    Adjust weights to ensure enriched results improve MRR without drowning out precise lexical matches.
  </action>
  <verify>
    <automated>tsx --test test/hybrid-search-enrichment.test.ts</automated>
  </verify>
  <done>Hybrid search utilizes enriched metadata for ranking, and conceptual search relevance is improved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Search Service | Loading enriched metadata from DB into search ranking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Elevation of Privilege | hybrid-ranking.ts | mitigate | Sanitize metadata content during search fusion to prevent injection into reranker prompts |
| T-02-03-02 | Tampering | schema.ts | mitigate | Ensure schema migrations are atomic and validated on startup |
</threat_model>

<success_criteria>
- AI-generated metadata is persisted in SQLite.
- Hybrid search results show improved relevance for queries targeting code intent rather than literal syntax.
- System handles chunks without enrichment gracefully (zero boost).
</success_criteria>

<output>
After completion, create `.planning/phases/phase-2/02-03-SUMMARY.md`
</output>
