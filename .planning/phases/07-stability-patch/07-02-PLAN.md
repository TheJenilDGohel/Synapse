---
phase: 07-stability-patch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/core/engine/memory/store/MemoryStore.ts, src/core/engine/retrieval/chunker/languages.ts]
autonomous: true
requirements: [MEM-01, RET-01]

must_haves:
  truths:
    - "MemoryStore sub-services are always initialized before being accessed"
    - "Tree-sitter language loading correctly extracts language objects from exports"
  artifacts:
    - path: "src/core/engine/memory/store/MemoryStore.ts"
      provides: "Lazy-initializing memory store methods"
    - path: "src/core/engine/retrieval/chunker/languages.ts"
      provides: "Robust language loaders"
  key_links:
    - from: "src/core/engine/memory/store/MemoryStore.ts"
      to: "this.init()"
      via: "await call at method entry"
---

<objective>
Ensure reliable initialization of the Memory Store and robust loading of Tree-sitter parsers.

Purpose: Prevent 'undefined' errors when accessing memory services and ensure semantic chunking works across supported languages.
Output: Patched MemoryStore.ts and languages.ts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/core/engine/memory/store/MemoryStore.ts
@src/core/engine/retrieval/chunker/languages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce MemoryStore initialization</name>
  <files>src/core/engine/memory/store/MemoryStore.ts</files>
  <action>
    - Review all public methods in `MemoryStore.ts`.
    - Add `await this.init();` at the beginning of every method that accesses `this.durableStore`, `this.kgService`, `this.taxonomyService`, `this.ingestionEngine`, or `this.adapter`.
    - Specifically check methods like `listEntries`, `getEntry`, `storeEntry`, `addEntity`, `listEntities`, etc.
    - Ensure `init()` remains idempotent (already checks `this.adapter`).
  </action>
  <verify>
    <automated>grep "async " src/core/engine/memory/store/MemoryStore.ts | wc -l</automated>
  </verify>
  <done>All memory store methods ensure initialization before use.</done>
</task>

<task type="auto">
  <name>Task 2: Fix tree-sitter language loading</name>
  <files>src/core/engine/retrieval/chunker/languages.ts</files>
  <action>
    - Update `LANGUAGE_LOADERS` in `languages.ts`.
    - For all languages (especially `typescript` and `tsx`), ensure the loader correctly handles both ESM and CJS export patterns.
    - Use a pattern like: `const pkg = mod.default || mod; const lang = pkg.typescript || pkg; return lang.language || lang;`
    - Apply similar defensive extraction to other loaders in the map.
  </action>
  <verify>
    <automated>node --check src/core/engine/retrieval/chunker/languages.ts</automated>
  </verify>
  <done>Tree-sitter loaders are robust against package export variations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App -> Memory | Internal database initialization |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Denial of Service | MemoryStore | mitigate | Ensure init() is idempotent and handles concurrent calls via its existing adapter check to avoid double-initialization locks. |
</threat_model>

<success_criteria>
- `MemoryStore` methods do not throw "cannot read property ... of undefined" on fresh instances.
- AST chunking initialization does not throw "language must be a tree-sitter language" or similar errors.
</success_criteria>

<output>
After completion, create `.planning/phases/07-stability-patch/07-02-SUMMARY.md`
</output>
