---
phase: 07-stability-patch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/interfaces/cli/commands/skill.ts, package.json]
autonomous: true
requirements: [SKILL-01, DEPS-01]

must_haves:
  truths:
    - "Skill CLI commands work on Windows without ESM path errors"
    - "@huggingface/transformers is listed in dependencies, not devDependencies"
  artifacts:
    - path: "src/interfaces/cli/commands/skill.ts"
      provides: "Windows-compatible ESM path handling"
    - path: "package.json"
      provides: "Correct production dependencies"
  key_links:
    - from: "src/interfaces/cli/commands/skill.ts"
      to: "scripts/runtime/install-loci-skill.mjs"
      via: "dynamic import with pathToFileURL"
---

<objective>
Resolve Windows compatibility issues in the Skill CLI and ensure production dependencies are correctly categorized.

Purpose: Prevent runtime crashes on Windows and deployment failures due to missing dependencies.
Output: Patched skill.ts and package.json.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/interfaces/cli/commands/skill.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ESM path error in skill.ts</name>
  <files>src/interfaces/cli/commands/skill.ts</files>
  <action>
    - Import `fileURLToPath` and `pathToFileURL` from `node:url`.
    - Replace `new URL(import.meta.url).pathname` with `fileURLToPath(import.meta.url)` to correctly handle Windows drives.
    - Use `pathToFileURL(SKILL_SCRIPT).href` in the dynamic `import()` call to ensure the path is treated as a valid URL by the ESM loader.
  </action>
  <verify>
    <automated>node --check src/interfaces/cli/commands/skill.ts</automated>
  </verify>
  <done>Skill CLI paths are Windows-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix production dependencies</name>
  <files>package.json</files>
  <action>
    - Verify if `@huggingface/transformers` is in `dependencies`.
    - If it is in `devDependencies`, move it to `dependencies`.
    - Remove it from `devDependencies` if present there.
    - Ensure it is not listed in `quality:deps` ignores if it's meant to be a tracked production dependency (unless strictly necessary).
  </action>
  <verify>
    <automated>grep -A 20 "dependencies" package.json | grep "@huggingface/transformers"</automated>
  </verify>
  <done>@huggingface/transformers is correctly categorized as a production dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI -> Script | Dynamic import of local script |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | skill.ts | mitigate | Use absolute paths resolved from import.meta.url to prevent loading from untrusted locations. |
</threat_model>

<success_criteria>
- `loci skill list` (or equivalent test command) does not throw "ERR_INVALID_URL" or path errors on Windows.
- `package.json` contains `@huggingface/transformers` in `dependencies`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-stability-patch/07-01-SUMMARY.md`
</output>
