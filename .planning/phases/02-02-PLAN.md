---
phase: 02-content-migration
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified: [
  "docs/docs/index.md",
  "docs/docs/architecture.md",
  "docs/docs/security.md",
  "docs/docs/changelog.md",
  "docs/docs/contributing.md",
  "docs/docs/project-overview.md",
  "docs/docusaurus.config.ts"
]
autonomous: true
requirements: ["RE-02"]

must_haves:
  truths:
    - "Root documentation files are present in docs/docs/ with MDX frontmatter."
    - "Comparison tables in the README use interactive Tabs components."
    - "Internal links between documents are functional within the Docusaurus site."
    - "Docusaurus site metadata (title, tagline) reflects the Synapse project."
  artifacts:
    - path: "docs/docs/index.md"
      provides: "Project landing page (from README.md)"
    - path: "docs/docs/architecture.md"
      provides: "System architecture documentation"
    - path: "docs/docs/security.md"
      provides: "Security policy and vulnerability disclosure"
    - path: "docs/docs/changelog.md"
      provides: "Release history"
    - path: "docs/docs/contributing.md"
      provides: "Contribution guidelines"
    - path: "docs/docs/project-overview.md"
      provides: "High-level project summary"
    - path: "docs/docusaurus.config.ts"
      provides: "Site configuration and navigation"
  key_links:
    - from: "docs/docs/index.md"
      to: "./architecture.md"
      via: "Markdown relative link"
    - from: "docs/docusaurus.config.ts"
      to: "docs/docs/index.md"
      via: "Navbar configuration"
---

<objective>
Port existing root Markdown documentation to the Docusaurus MDX structure, enhancing it with interactive components and ensuring correct navigation and metadata.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M002-ROADMAP.md
@.planning/STATE.md
@README.md
@ARCHITECTURE.md
@SECURITY.md
@CHANGELOG.md
@CONTRIBUTING.md
@PROJECT.md
@docs/docusaurus.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content Migration & Metadata Cleanup</name>
  <files>
    docs/docs/index.md,
    docs/docs/architecture.md,
    docs/docs/security.md,
    docs/docs/changelog.md,
    docs/docs/contributing.md,
    docs/docs/project-overview.md,
    docs/docusaurus.config.ts
  </files>
  <action>
    1. **Copy & Rename**: Port root Markdown files to `docs/docs/`:
       - `README.md` -> `docs/docs/index.md`
       - `ARCHITECTURE.md` -> `docs/docs/architecture.md`
       - `SECURITY.md` -> `docs/docs/security.md`
       - `CHANGELOG.md` -> `docs/docs/changelog.md`
       - `CONTRIBUTING.md` -> `docs/docs/contributing.md`
       - `PROJECT.md` -> `docs/docs/project-overview.md`
    2. **Add Frontmatter**: Add Docusaurus frontmatter to each file (id, title, sidebar_label). 
       - For `index.md`, set `slug: /`.
    3. **Cleanup Default Docs**: Delete `docs/docs/intro.mdx` and the `tutorial-basics`/`tutorial-extras` directories.
    4. **Update Configuration**: Modify `docs/docusaurus.config.ts`:
       - Set `title` to 'Synapse'.
       - Set `tagline` to 'Bridging Context for AI Agents'.
       - Set `url` to 'https://thejenildgohel.github.io' (per project structure).
       - Set `baseUrl` to '/synapse/'.
       - Update `navbar` labels: Change 'Tutorial' to 'Docs'.
  </action>
  <verify>
    <automated>
      ls docs/docs/index.md docs/docs/architecture.md docs/docs/security.md docs/docs/changelog.md docs/docs/contributing.md docs/docs/project-overview.md
    </automated>
  </verify>
  <done>
    Files migrated with frontmatter, default tutorials removed, and site metadata updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: MDX Integration & Interactive Components</name>
  <files>docs/docs/index.md</files>
  <action>
    1. **Tabs for Comparisons**: In `docs/docs/index.md`, refactor the "vs Memory-Only Servers" and "vs Code Intelligence Servers" tables. 
       - Use `<Tabs>` and `<TabItem>` components from `@theme/Tabs` and `@theme/TabItem`.
       - Place the comparison tables inside separate tabs.
    2. **Admonitions**: Convert troubleshooting notes and specialized warnings (e.g., the Mem0 star note) into Docusaurus Admonitions (`:::info`, `:::tip`, `:::caution`).
    3. **Tool Suites Refactor**: Optimize the tool suites `<details>` blocks. Ensure they look correct in MDX or use MDX equivalents if better.
    4. **Import Components**: Ensure `import Tabs from '@theme/Tabs';` and `import TabItem from '@theme/TabItem';` are added to the top of `index.md`.
  </action>
  <verify>
    <automated>
      grep -q "<Tabs" docs/docs/index.md && grep -q "import Tabs" docs/docs/index.md
    </automated>
  </verify>
  <done>
    Index page utilizes MDX Tabs and Admonitions for a more interactive experience.
  </done>
</task>

<task type="auto">
  <name>Task 3: Link Resolution & Navigation Audit</name>
  <files>docs/docs/index.md, docs/docs/architecture.md, docs/docs/security.md, docs/docs/changelog.md, docs/docs/contributing.md, docs/docs/project-overview.md</files>
  <action>
    1. **Link Migration**: Update all relative Markdown links in the migrated files to match the new Docusaurus structure.
       - Example: `[Changelog](./CHANGELOG.md)` -> `[Changelog](./changelog.md)` or `[Changelog](/docs/changelog)`.
    2. **External Link Check**: Ensure links pointing to the previous GitHub Pages site are updated to point to the new relative paths where appropriate.
    3. **Sidebar Label Consistency**: Verify that the `sidebar_label` in frontmatter matches the intended navigation structure.
  </action>
  <verify>
    <automated>
      # Search for any remaining .md links that should be lowercase or relative to docs
      grep -r "\](\./.*\.md)" docs/docs/
    </automated>
  </verify>
  <done>
    Internal documentation links are resolved and functional.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Docs Site | Untrusted user browsing documentation |
| Docs Build -> GitHub Pages | Automated deployment of documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | docs/docs/*.md | mitigate | Enforce OIDC Trusted Publishing for site deployments |
| T-02-02-02 | Information Disclosure | docs/docs/security.md | accept | Security policy is public by design |
</threat_model>

<verification>
Run `npm run docs:build` from the root to ensure all MDX components are correctly imported and links resolve during the static build process.
</verification>

<success_criteria>
1. All 6 core documents are accessible at `/docs/` paths.
2. The index page features interactive tabs for competitor comparisons.
3. Troubleshooting and notes use visually distinct Admonition blocks.
4. Site navigation correctly displays 'Docs' instead of 'Tutorial'.
</success_criteria>

<output>
After completion, create `.planning/phases/02-02-SUMMARY.md`
</output>
