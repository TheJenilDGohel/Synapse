---
phase: 01-stabilization-and-scaling
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/core/engine/retrieval/sqlite-vec/runtime.ts, src/core/engine/retrieval/sqlite-vec/service.ts, src/core/engine/retrieval/sqlite-vec/schema.ts]
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "Vector virtual table uses 'vec1' instead of 'vec0'"
    - "System automatically migrates data from vec0 (or JSON) to vec1 upon first run"
    - "IVF indexing is configured on the vec1 table"
  artifacts:
    - path: "src/core/engine/retrieval/sqlite-vec/runtime.ts"
      provides: "vec1 table definition and IVF configuration"
    - path: "src/core/engine/retrieval/sqlite-vec/schema.ts"
      provides: "Migration logic for vec1 transition"
  key_links:
    - from: "src/core/engine/retrieval/sqlite-vec/service.ts"
      to: "vec1 table"
      via: "SQL queries"
---

<objective>
Transition the semantic backend from `vec0` (legacy) and JSON fallbacks to the high-performance `vec1` virtual table. This enables advanced scaling features like IVF (Inverted File) indexing, which is critical for supporting 10M+ vectors (PERF-02).

Purpose: Future-proof the vector store and improve search performance at scale.
Output: Updated schema, migration logic, and IVF-enabled vector search.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M000-CONTEXT.md
@.planning/M001-ROADMAP.md
@.planning/STATE.md
@src/core/engine/retrieval/sqlite-vec/runtime.ts
@src/core/engine/retrieval/sqlite-vec/service.ts
@src/core/engine/retrieval/sqlite-vec/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update schema to vec1 and configure IVF indexing</name>
  <files>src/core/engine/retrieval/sqlite-vec/runtime.ts</files>
  <behavior>
    - `getVecTableDefinition` returns a `vec1` table definition.
    - `ensureSqliteVecTable` creates the `vec1` table and an IVF index (e.g., `ivf8`) on the embedding column.
    - `shouldRebuildVecTable` correctly identifies if a `vec0` table exists and needs replacement.
  </behavior>
  <action>
    1. Change `vec0` to `vec1` in `getVecTableDefinition`.
    2. Add `CREATE INDEX ... USING ivf8` to `ensureSqliteVecTable`.
    3. Update `shouldRebuildVecTable` to detect `vec0` schema and trigger a rebuild.
  </action>
  <verify>
    <automated>npm test test/sqlite-vec-index-service.test.js</automated>
  </verify>
  <done>vec1 is the active virtual table type; IVF index is created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement migration path from JSON and vec0</name>
  <files>src/core/engine/retrieval/sqlite-vec/service.ts, src/core/engine/retrieval/sqlite-vec/schema.ts</files>
  <action>
    1. In `SqliteVecIndexService.ensureDb`, add logic to check for a legacy `index.json` file.
    2. If found, and the SQLite DB is empty, perform a one-time migration: load JSON -> insert into SQLite.
    3. Ensure `syncSqliteVecRowsFromChunks` is called after migration to populate the `vec1` table.
    4. Rename/Archive the `index.json` file after successful migration.
  </action>
  <verify>
    <automated>npm test test/vector-index-service.test.js</automated>
  </verify>
  <done>Legacy JSON data is automatically migrated to SQLite vec1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON → SQLite | Data migration boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | index.json | mitigate | Delete or move `index.json` to a protected directory after migration to prevent accidental exposure of raw vectors. |
</threat_model>

<success_criteria>
- Virtual table is upgraded to `vec1`.
- Existing users on JSON backend are seamlessly migrated to SQLite.
- IVF indexing is active for faster retrieval.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization-and-scaling/01-02-SUMMARY.md`
</output>
