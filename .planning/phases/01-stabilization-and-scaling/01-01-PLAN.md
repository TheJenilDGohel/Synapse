---
phase: 01-stabilization-and-scaling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, .github/workflows/platform-verification.yml, src/core/engine/retrieval/sqlite-vec/runtime.ts]
autonomous: true
requirements: [STAB-01]

must_haves:
  truths:
    - "sqlite-vec is listed in package.json dependencies"
    - "CI workflow successfully loads sqlite-vec across Ubuntu, macOS, and Windows"
    - "SqliteVecIndexService uses getLoadablePath() instead of hardcoded/manual paths"
  artifacts:
    - path: ".github/workflows/platform-verification.yml"
      provides: "Cross-platform verification of SQLite extensions"
    - path: "src/core/engine/retrieval/sqlite-vec/runtime.ts"
      provides: "Unified extension loading logic"
  key_links:
    - from: "src/core/engine/retrieval/sqlite-vec/runtime.ts"
      to: "sqlite-vec"
      via: "import and getLoadablePath()"
---

<objective>
Establish a stable, cross-platform foundation for the SQLite vector backend. This involves automating verification via CI and refactoring the extension loading logic to use standard, reliable methods provided by the official `sqlite-vec` package.

Purpose: Fix "binary not found" issues and ensure reliability across developer environments (STAB-01).
Output: Updated dependencies, new CI workflow, and refactored runtime loading.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M000-CONTEXT.md
@.planning/M001-ROADMAP.md
@.planning/STATE.md
@src/core/engine/retrieval/sqlite-vec/runtime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sqlite-vec dependency and setup CI matrix</name>
  <files>package.json, .github/workflows/platform-verification.yml</files>
  <action>
    1. Add `sqlite-vec` (latest stable) to `package.json` dependencies.
    2. Create `.github/workflows/platform-verification.yml` with a matrix for `os: [ubuntu-latest, macos-latest, windows-latest]` and `node-version: [22]`.
    3. The CI job should run a small script that attempts to `import { getLoadablePath } from "sqlite-vec"` and load it into `node:sqlite`.
  </action>
  <verify>
    <automated>npm install && node -e "import('sqlite-vec').then(m => console.log(m.getLoadablePath()))"</automated>
  </verify>
  <done>sqlite-vec is in package.json and CI workflow is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor runtime loading to use getLoadablePath()</name>
  <files>src/core/engine/retrieval/sqlite-vec/runtime.ts</files>
  <behavior>
    - If `sqlite-vec` is available in node_modules, `tryLoadSqliteVec` should use `getLoadablePath()`.
    - Fallback to manually provided `sqliteVecExtensionPath` if `getLoadablePath()` fails or returns null.
    - Ensure `enableLoadExtension(true)` is called before `loadExtension`.
  </behavior>
  <action>
    1. Import `getLoadablePath` from `sqlite-vec`.
    2. Update `tryLoadSqliteVec` to prioritize the result of `getLoadablePath()`.
    3. Maintain backward compatibility for users who manually provide a path via configuration.
  </action>
  <verify>
    <automated>npm test test/sqlite-vec-index-service.test.js</automated>
  </verify>
  <done>runtime.ts uses official loading logic; existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System → SQLite | Native binary loading occurs here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | sqlite-vec binary | mitigate | Rely on npm integrity checks (package-lock.json) for the native extension. |
</threat_model>

<success_criteria>
- `sqlite-vec` is a first-class dependency.
- Extension loading is deterministic and cross-platform.
- CI verifies stability across major OS targets.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization-and-scaling/01-01-SUMMARY.md`
</output>
