---
phase: 01-stabilization-and-scaling
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [scripts/quality/stress-synapse.mjs, src/core/engine/retrieval/sqlite-vec/semantic-search.ts]
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "System can perform semantic search across 10 million vectors"
    - "Search latency is documented and remains within acceptable limits (<500ms)"
    - "IVF 'probes' parameter is tunable to balance speed and recall"
  artifacts:
    - path: "scripts/quality/stress-synapse.mjs"
      provides: "10M vector generation and benchmarking"
    - path: "src/core/engine/retrieval/sqlite-vec/semantic-search.ts"
      provides: "Optimized search implementation with IVF tuning"
  key_links:
    - from: "src/core/engine/retrieval/sqlite-vec/semantic-search.ts"
      to: "vec1 table"
      via: "SQL query with 'probes' parameter"
---

<objective>
Verify and optimize the semantic search engine for massive scale. By generating 10 million synthetic vectors and benchmarking search latency, we will identify the optimal IVF (Inverted File) "probe" counts to maintain high recall without sacrificing speed (PERF-01).

Purpose: Ensure Synapse remains responsive even for the largest codebases.
Output: Benchmarking results and tuned search parameters.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M000-CONTEXT.md
@.planning/M001-ROADMAP.md
@.planning/STATE.md
@scripts/quality/stress-synapse.mjs
@src/core/engine/retrieval/sqlite-vec/semantic-search.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Benchmark 10M vector search performance</name>
  <files>scripts/quality/stress-synapse.mjs</files>
  <action>
    1. Update `stress-synapse.mjs` to include a mode for generating and indexing 10 million synthetic embeddings (384 dimensions).
    2. Add a benchmarking suite that measures:
       - Time to build the IVF index.
       - Average search latency (K=10).
       - Recall vs. brute-force (for a subset of queries).
  </action>
  <verify>
    <automated>node scripts/quality/stress-synapse.mjs --count 10000000 --benchmark</automated>
  </verify>
  <done>Benchmarking tool is capable of measuring 10M vector performance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tune IVF probe counts for speed/recall balance</name>
  <files>src/core/engine/retrieval/sqlite-vec/semantic-search.ts</files>
  <behavior>
    - The SQL query for `vec1` search should include the `probes` parameter (e.g., `probes=10`).
    - The system should support an environment variable or config option `SYNAPSE_VEC_PROBES` to override the default.
    - Default `probes` should be set based on benchmark findings to hit <500ms target.
  </behavior>
  <action>
    1. Update the `semanticSearch` SQL query in `semantic-search.ts` to utilize the `probes` parameter available in `vec1`.
    2. Implement logic to determine the optimal probe count based on the total number of chunks (e.g., higher probes for larger indices).
  </action>
  <verify>
    <automated>npm test test/sqlite-vec-index-service.test.js</automated>
  </verify>
  <done>Search implementation is optimized for large-scale retrieval using IVF probes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configuration → SQL | Probe count parameter injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | probes parameter | mitigate | Limit the maximum value of `probes` to 100 to prevent CPU exhaustion from overly exhaustive searches. |
</threat_model>

<success_criteria>
- 10M vector search is verified and stable.
- Latency target of <500ms is achieved for large indices.
- IVF parameters are tuned for the best trade-off between speed and recall.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization-and-scaling/01-03-SUMMARY.md`
</output>
