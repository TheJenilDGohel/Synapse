---
phase: 04-architectural-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/interfaces/services.ts, src/core/engine/retrieval/chunker/ast-utils.ts, src/interfaces/mcp/utils/response-mapper.ts]
autonomous: true
requirements: [ARCH-01, PERF-01, DRY-01]

must_haves:
  truths:
    - "Service interfaces are centralized and strictly typed"
    - "AST chunking uses memoization for scope path resolution"
    - "McpResponseMapper handles formatting parameters consistently"
  artifacts:
    - path: "src/core/interfaces/services.ts"
      provides: "Centralized service contracts"
    - path: "src/core/engine/retrieval/chunker/ast-utils.ts"
      provides: "Optimized AST traversal"
    - path: "src/interfaces/mcp/utils/response-mapper.ts"
      provides: "DRY response formatting utility"
  key_links:
    - from: "src/core/engine/retrieval/chunker/ast-utils.ts"
      to: "memoization map"
      via: "single-pass traversal"
---

<objective>
Establish the architectural foundation for Phase 04 by centralizing service interfaces, optimizing performance-critical AST traversal, and creating a shared utility for MCP response mapping.

Purpose: Reduce cognitive load, improve performance for large files, and eliminate duplication in tool handlers.
Output: Updated interfaces, optimized ast-utils, and a new McpResponseMapper.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/core/interfaces/services.ts
@src/core/engine/retrieval/chunker/ast-utils.ts
@src/interfaces/mcp/tools/memory-store.ts
@.planning/architecture/HARDENING-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize Service Interfaces</name>
  <files>src/core/interfaces/services.ts</files>
  <action>
    - Extract and centralize redundant 'MemoryService' and other service interface definitions from tool files (e.g., 'memory-store.ts', 'graph-tools.ts') into 'src/core/interfaces/services.ts'.
    - Ensure all methods used by tools are represented in the core interfaces.
    - Add 'IDurableStore', 'IKnowledgeGraphService', 'ITaxonomyService', and 'IIngestionEngine' interfaces as placeholders for the upcoming decomposition.
  </action>
  <verify>
    <automated>npm run build:check</automated>
  </verify>
  <done>Interfaces centralized and baseline build passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Optimize AST Chunking</name>
  <files>src/core/engine/retrieval/chunker/ast-utils.ts</files>
  <behavior>
    - 'getNodeName' should use a cache if provided to avoid redundant DFS/lookups.
    - 'buildScopePath' should be refactored to optionally accept a pre-computed NameMap to achieve O(1) lookups during traversal.
    - Add a new function 'buildASTRenderContext(root: TreeSitterNode)' that performs a single pass to build the Node -> Name mapping.
  </behavior>
  <action>
    - Implement the optimizations in 'ast-utils.ts' as per Hardening Report 3.1.
    - Use a WeakMap for memoization to prevent memory leaks while caching node names.
  </action>
  <verify>
    <automated>npm test -- --filter=ast-utils</automated>
  </verify>
  <done>AST traversal is optimized and passes tests.</done>
</task>

<task type="auto">
  <name>Task 3: Create McpResponseMapper Utility</name>
  <files>src/interfaces/mcp/utils/response-mapper.ts</files>
  <action>
    - Create 'McpResponseMapper' class that encapsulates 'terse' and 'item_format' logic (Hardening Report 1.1).
    - Move logic from 'terse-utils.ts' into this mapper if appropriate, or wrap it.
    - It should accept a raw result and parameters, returning the formatted MCP content.
  </action>
  <verify>
    <automated>npm run build:check</automated>
  </verify>
  <done>McpResponseMapper utility created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| McpResponseMapper | Input from tools (user parameters) must be validated (Zod already handles this, but mapper must be robust to unexpected shapes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | McpResponseMapper | mitigate | Ensure 'lite' and 'compact' modes strictly filter fields and do not leak internal metadata. |
| T-04-02 | Denial of Service | ast-utils | mitigate | Optimization reduces CPU usage on large files, mitigating DoS via complex ASTs. |
</threat_model>

<verification>
Check centralized interfaces for completeness. Run AST benchmarks if available. Verify McpResponseMapper can handle various result shapes.
</verification>

<success_criteria>
- Build passes without 'as any' where interfaces are now properly defined.
- AST traversal complexity reduced from effectively quadratic to linear.
- Duplicated response logic in tool files is ready to be replaced by the mapper.
</success_criteria>

<output>
After completion, create `.planning/phases/04-architectural-hardening/04-01-SUMMARY.md`
</output>
