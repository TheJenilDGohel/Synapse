---
phase: 04-architectural-hardening
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/core/engine/memory/store/MemoryStore.ts, src/core/engine/memory/store/DurableStore.ts, src/core/engine/memory/knowledge-graph/KnowledgeGraphService.ts, src/core/engine/memory/taxonomy/TaxonomyService.ts, src/core/engine/memory/knowledge-graph/graph.ts, src/interfaces/mcp/tools/memory-store.ts, src/interfaces/mcp/tools/graph-tools.ts]
autonomous: true
requirements: [ARCH-02, PERF-02, DRY-01]

must_haves:
  truths:
    - "MemoryStore logic is distributed among specialized services"
    - "Graph traversal does not use expensive string-based cycle detection"
    - "Tool handlers use McpResponseMapper for all outputs"
  artifacts:
    - path: "src/core/engine/memory/store/DurableStore.ts"
      provides: "CRUD for memories"
    - path: "src/core/engine/memory/knowledge-graph/KnowledgeGraphService.ts"
      provides: "Graph operations"
    - path: "src/core/engine/memory/taxonomy/TaxonomyService.ts"
      provides: "Taxonomy management"
  key_links:
    - from: "src/interfaces/mcp/tools/memory-store.ts"
      to: "McpResponseMapper"
      via: "standardized formatting calls"
---

<objective>
Decompose the MemoryStore God Object into specialized, single-responsibility services. Optimize the performance of recursive graph queries and unify tool response formatting using the McpResponseMapper.

Purpose: Improve maintainability (SRP), performance, and consistency.
Output: Specialized services and normalized tool handlers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/core/engine/memory/store/MemoryStore.ts
@src/core/engine/memory/knowledge-graph/graph.ts
@src/interfaces/mcp/tools/memory-store.ts
@src/interfaces/mcp/tools/graph-tools.ts
@.planning/phases/04-architectural-hardening/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memory Subsystem Decomposition</name>
  <files>src/core/engine/memory/store/DurableStore.ts, src/core/engine/memory/knowledge-graph/KnowledgeGraphService.ts, src/core/engine/memory/taxonomy/TaxonomyService.ts, src/core/engine/memory/store/MemoryStore.ts</files>
  <action>
    - Move CRUD logic from 'MemoryStore.ts' to 'DurableStore.ts'.
    - Move entity/triple/graph logic to 'KnowledgeGraphService.ts'.
    - Move nest/branch logic to 'TaxonomyService.ts'.
    - Refactor 'MemoryStore.ts' to be a facade that instantiates or receives these services and delegates to them.
  </action>
  <verify>
    <automated>npm test -- --filter=memory-store</automated>
  </verify>
  <done>Memory system decomposed into specialized services.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize Graph Traversal</name>
  <files>src/core/engine/memory/knowledge-graph/graph.ts, src/core/engine/memory/knowledge-graph/KnowledgeGraphService.ts</files>
  <action>
    - Refactor 'traverseGraph' in 'graph.ts' to use SQLite's native cycle detection ('CYCLE' clause if supported) or optimize the visited set logic.
    - If native 'CYCLE' is not available (SQLite < 3.34), move cycle detection to application layer or use a more efficient bitmask/hash if possible within SQL.
    - Per Hardening Report 3.2, eliminate 'INSTR' string manipulation for cycle detection.
  </action>
  <verify>
    <automated>npm test -- --filter=graph</automated>
  </verify>
  <done>Graph traversal optimized.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Tool Handlers for DRY Response Mapping</name>
  <files>src/interfaces/mcp/tools/memory-store.ts, src/interfaces/mcp/tools/graph-tools.ts</files>
  <action>
    - Refactor 'memory-store.ts' and 'graph-tools.ts' handlers to use 'McpResponseMapper' created in Plan 01.
    - Eliminate manual calls to 'applyReadFormatToItems' and 'toMinimalWriteResponse' within individual handlers.
    - Reference centralized interfaces for the 'memory' and 'graph' services.
  </action>
  <verify>
    <automated>npm run build:check</automated>
  </verify>
  <done>Tool handlers normalized and DRY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KnowledgeGraphService | Database queries must continue to use parameterized inputs to prevent SQL injection during traversal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | KnowledgeGraphService | mitigate | Limit recursion depth and breadth to prevent resource exhaustion during complex traversals. |
| T-04-04 | Tampering | DurableStore | mitigate | Ensure service decomposition doesn't bypass existing validation logic for memory entries. |
</threat_model>

<verification>
Run all memory and graph tool tests. Verify 'lite' and 'compact' modes still work as expected in the UI/Agent.
</verification>

<success_criteria>
- MemoryStore is no longer a God Object.
- Graph traversal performance improved for deep queries.
- Tool handlers are significantly shorter and easier to maintain.
</success_criteria>

<output>
After completion, create `.planning/phases/04-architectural-hardening/04-02-SUMMARY.md`
</output>
