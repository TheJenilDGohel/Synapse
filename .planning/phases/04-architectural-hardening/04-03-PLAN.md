---
phase: 04-architectural-hardening
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [src/core/runtime/registry.ts, src/core/runtime/config.ts, src/interfaces/app/mcp-server.ts, src/core/engine/memory/service.ts, src/core/engine/retrieval/embedding/service.ts, src/core/engine/retrieval/search/service.ts]
autonomous: true
requirements: [SOLID-01, ARCH-01]

must_haves:
  truths:
    - "CoreServiceRegistry accepts RuntimeConfig in constructor"
    - "All core services use constructor-based dependency injection"
    - "No 'as any' or 'as unknown as' casts for core service instances"
  artifacts:
    - path: "src/core/runtime/registry.ts"
      provides: "Strictly-typed DI container"
  key_links:
    - from: "src/interfaces/app/mcp-server.ts"
      to: "CoreServiceRegistry"
      via: "manual instantiation with config"
---

<objective>
Complete the architectural hardening by refactoring the CoreServiceRegistry to use Dependency Inversion (DIP). Shift from pull-based configuration to push-based injection, eliminating type casts and hardcoded instantiations.

Purpose: Improve testability, type safety, and decouple service creation from configuration retrieval.
Output: Refactored DI container and strictly typed service graph.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/core/runtime/registry.ts
@src/core/runtime/config.ts
@src/interfaces/app/mcp-server.ts
@.planning/phases/04-architectural-hardening/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CoreServiceRegistry & RuntimeConfig Refactor</name>
  <files>src/core/runtime/registry.ts, src/core/runtime/config.ts</files>
  <action>
    - Refactor 'CoreServiceRegistry' class to accept 'RuntimeConfig' as a constructor parameter (Hardening Report 2.2).
    - Remove calls to 'buildRuntimeConfig()' from within service getters.
    - Ensure 'RuntimeConfig' type is exported and clean.
  </action>
  <verify>
    <automated>npm run build:check</automated>
  </verify>
  <done>Registry refactored to accept configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Constructor-Based Injection</name>
  <files>src/core/runtime/registry.ts, src/core/engine/memory/service.ts, src/core/engine/retrieval/embedding/service.ts, src/core/engine/retrieval/search/service.ts</files>
  <action>
    - Update 'MemoryService', 'EmbeddingService', and 'SearchService' to accept their dependencies via constructor instead of creating them or pulling them from a global registry.
    - Wire these services in 'CoreServiceRegistry' using the provided 'RuntimeConfig'.
    - Eliminate all 'as any' and 'as unknown as' casts in the registry (Hardening Report 2.2).
    - Address circular risks (e.g., SearchService needing VectorIndex from MemoryService) by using interfaces or late-binding setters where strictly necessary, but prefer constructor injection.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Constructor-based injection implemented and type safety enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Update Entry Point for DI</name>
  <files>src/interfaces/app/mcp-server.ts</files>
  <action>
    - Update 'mcp-server.ts' (or the primary entry point) to build the 'RuntimeConfig' first.
    - Instantiate 'CoreServiceRegistry' with the config and pass the registry/services to tool registration functions.
    - Ensure the global 'services' singleton is either removed or properly initialized before use.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>System entry point updated to support dependency injection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CoreServiceRegistry | Configuration pushed from entry point must be validated (buildRuntimeConfig already does this). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | CoreServiceRegistry | mitigate | Ensure sensitive config (API keys) is only passed to services that need it. |
</threat_model>

<verification>
Ensure all core tests pass. Verify that services can be instantiated in isolation for unit testing.
</verification>

<success_criteria>
- CoreServiceRegistry is a proper DI container.
- Zero 'as any' casts in the core registry.
- Services are decoupled from global state and configuration retrieval logic.
</success_criteria>

<output>
After completion, create `.planning/phases/04-architectural-hardening/04-03-SUMMARY.md`
</output>
