---
phase: 10-tool-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interfaces/mcp/tools/memory-store.ts
  - src/interfaces/mcp/tools/memory-workflow.ts
  - src/interfaces/mcp/tools/retrieval.ts
  - src/interfaces/mcp/tools/retrieval-workspace.ts
  - src/interfaces/mcp/tools/symbol-tools.ts
  - src/interfaces/mcp/tools/audit-tools.ts
  - src/interfaces/mcp/tools/backfill-tools.ts
  - src/interfaces/mcp/tools/backup-tools.ts
  - src/interfaces/mcp/tools/find-tools.ts
  - src/interfaces/mcp/tools/core.ts
  - src/interfaces/mcp/tools/index.ts
autonomous: true
requirements: [TOOL-CON-01, TOOL-CON-02]
must_haves:
  truths:
    - "All granular MCP tools are consolidated into Power Controllers"
    - "Token overhead of the MCP schema is reduced by >50%"
    - "Existing functionality is preserved exactly in the new controllers"
  artifacts:
    - path: "src/interfaces/mcp/tools/memory-store.ts"
      provides: "synapse_memory_manage & synapse_memory_query"
    - path: "src/interfaces/mcp/tools/retrieval.ts"
      provides: "synapse_search"
    - path: "src/interfaces/mcp/tools/symbol-tools.ts"
      provides: "synapse_symbol_query"
    - path: "src/interfaces/mcp/tools/retrieval-workspace.ts"
      provides: "synapse_workspace_manage"
    - path: "src/interfaces/mcp/tools/core.ts"
      provides: "synapse_system_manage"

---

<objective>
Consolidate the remaining granular MCP tools into a focused set of Power Controllers using Zod discriminated unions. This will reduce the context window "tax" of the tool definitions while maintaining full operational capabilities.

Purpose: Optimize for context-efficiency in long-running agent sessions.
Output: Refactored MCP tool modules and a streamlined schema.
</objective>

<execution_context>
@.planning/phases/10-tool-consolidation/01-RESEARCH.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate Memory Tools</name>
  <files>src/interfaces/mcp/tools/memory-store.ts, src/interfaces/mcp/tools/memory-workflow.ts</files>
  <action>
    Refactor memory tools into:
    - `synapse_memory_manage`: (store, update, delete, capture_event, add_relation, remove_relation, store_batch, teach, capture_outcome).
    - `synapse_memory_query`: (list, get, events, suggest, related, status, recall, task_context, whats_new).
    
    Implementation:
    1. Merge logic from `memory-workflow.ts` into `memory-store.ts` where appropriate or keep separate if cleaner but export under the same tool name.
    2. Use `z.discriminatedUnion('action', [...])`.
    3. Update `registerMemoryStoreTools` and `registerMemoryWorkflowTools`.
  </action>
  <verify>
    <automated>npm run build && npx tsx bin/loci.js mcp status --verbose</automated>
  </verify>
  <done>Memory tools consolidated.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate Search and Symbol Tools</name>
  <files>src/interfaces/mcp/tools/retrieval.ts, src/interfaces/mcp/tools/symbol-tools.ts, src/interfaces/mcp/tools/find-tools.ts</files>
  <action>
    Refactor into:
    - `synapse_search`: (files, code, hybrid, find).
    - `synapse_symbol_query`: (callers, definition, implementations, usages, get, rename_preview).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Search and Symbol tools consolidated.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate Workspace and System Tools</name>
  <files>src/interfaces/mcp/tools/retrieval-workspace.ts, src/interfaces/mcp/tools/core.ts, src/interfaces/mcp/tools/audit-tools.ts, src/interfaces/mcp/tools/backfill-tools.ts, src/interfaces/mcp/tools/backup-tools.ts</files>
  <action>
    Refactor into:
    - `synapse_workspace_manage`: (list_roots, list_projects, tree, read, file_changed, summarize, backfill).
    - `synapse_system_manage`: (index_status, embed_status, index_project, audit, backup, restore, update_status, update_self).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Workspace and System tools consolidated.</done>
</task>

<task type="auto">
  <name>Task 4: Update Registry and Discovery</name>
  <files>src/interfaces/mcp/tools/core.ts, src/interfaces/mcp/common/status.ts</files>
  <action>
    1. Update `synapse_discovery` to reflect new Power Controllers.
    2. Update `buildHelpGuide` in `status.ts` to suggest the new consolidated tools.
  </action>
  <verify>
    <automated>npx tsx bin/loci.js synapse_help --task "search code"</automated>
  </verify>
  <done>Discovery logic updated.</done>
</task>

<task type="auto">
  <name>Task 5: Final Cleanup and Documentation</name>
  <files>src/interfaces/mcp/tools/index.ts, README.md, CHANGELOG.md</files>
  <action>
    1. Remove any orphaned tool files.
    2. Update `src/interfaces/mcp/tools/index.ts`.
    3. Update `README.md` and `CHANGELOG.md`.
    4. Update version badge in docs if needed (though already done).
  </action>
  <verify>
    <automated>npm run build && npm run doctor</automated>
  </verify>
  <done>Cleanup complete.</done>
</task>

</tasks>

<threat_model>
- Boundary: LLM → Power Controller.
- Threat: Incorrect action mapping or parameter injection.
- Mitigation: Strict Zod validation on the union discriminator and action-specific sub-schemas.
</threat_model>

<success_criteria>
- Tool count reduced from ~72 to < 20.
- All original capabilities verified functional.
- Build and tests pass.
</success_criteria>
