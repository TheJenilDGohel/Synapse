---
phase: 04-astro-docs
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified: [docs/src/content/docs/**]
---

# 04-02: Content Migration

## Objective
Migrate all technical documentation into the Astro Starlight site structure, ensuring all 74 tools and core pillars are accurately documented.

## Implementation Steps
1. **Core Pillar Documentation**:
   - Fill `pillars/code-intelligence.md` using content from `src/core/engine/retrieval/README.md` (if exists) or `ARCHITECTURE.md`.
   - Fill `pillars/knowledge-graph.md` using `src/core/engine/kg/README.md` or `ARCHITECTURE.md`.
   - Fill `pillars/ai-memory.md` using `src/core/engine/memory/README.md` or `ARCHITECTURE.md`.
2. **Advanced Section**:
   - Document `advanced/custom-skills.md` using `SKILL.md`.
   - Document `advanced/hooks.md` and `advanced/security.md` using `ARCHITECTURE.md` and `SECURITY.md`.
3. **Tool Reference**:
   - Create a script to extract tool metadata from `src/interfaces/mcp/tools.ts` (or similar) and generate `reference/*.md` files.
   - For now, I'll manually migrate the most important tools.

## Verification & Testing
- `npm run docs:build` should succeed.
- Verify all links between pages are functional.
