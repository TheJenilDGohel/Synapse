---
phase: 08-cli-refinement
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/interfaces/cli/parse-flags.ts, src/interfaces/cli/commands/memory.ts, src/interfaces/cli/commands/kg.ts, src/interfaces/cli/commands/onboard.ts]
autonomous: true
requirements: [SUBCOMMAND-HELP]
must_haves:
  truths:
    - "loci memory add --help displays help instead of an error"
    - "loci kg query -h displays help instead of an error"
  artifacts:
    - path: "src/interfaces/cli/parse-flags.ts"
      provides: "helpRequested detection"
---

<objective>
Enable subcommand-level help handling across the CLI. This ensures that users running commands like `loci memory add --help` see the relevant help text instead of being prompted for missing arguments or seeing validation errors.

Purpose: Improve CLI usability and discoverability.
Output: Updated parse-flags.ts and key subcommand handlers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/interfaces/cli/parse-flags.ts
@src/interfaces/cli/commands/memory.ts
@src/interfaces/cli/commands/kg.ts
@src/interfaces/cli/help.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update parseFlags to detect help requests</name>
  <files>src/interfaces/cli/parse-flags.ts</files>
  <action>
    Modify `parseFlags` in `src/interfaces/cli/parse-flags.ts` to:
    1. Scan the input `args` for `--help` or `-h`.
    2. If found, set a new `helpRequested: true` property in the returned object.
    3. Ensure it doesn't consume the flag in a way that breaks other logic if we decide to handle it elsewhere, but returning it in the result is the primary goal.
    4. Update the `ParsedFlags` interface to include `helpRequested: boolean`.
  </action>
  <verify>
    <automated>node -e "const { parseFlags } = require('./src/interfaces/cli/parse-flags.js'); console.log(parseFlags(['--help'], {}).helpRequested)" (Should be true)</automated>
  </verify>
  <done>parseFlags correctly identifies help requests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement help check in subcommand handlers</name>
  <files>src/interfaces/cli/commands/memory.ts, src/interfaces/cli/commands/kg.ts, src/interfaces/cli/commands/onboard.ts, src/interfaces/cli/commands/mcp.ts, src/interfaces/cli/commands/skill.ts</files>
  <action>
    For each major subcommand handler:
    1. Check `helpRequested` from the `parseFlags` result.
    2. If true, print the usage/help text for that specific subcommand and return early.
    3. You can reuse `printSubcommandHelp` or provide a more specific one-liner if appropriate.
    
    Priority files: `memory.ts`, `kg.ts`, `onboard.ts`.
  </action>
  <verify>
    <automated>loci memory add --help</automated>
  </verify>
  <done>Subcommands respond correctly to help flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> CLI | Command line arguments are parsed; help requests are short-circuiting logic. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Denial of Service | parse-flags.ts | mitigate | Ensure flag parsing is efficient and doesn't backtrack excessively on large inputs. |
</threat_model>

<verification>
Test the following commands:
- loci memory add --help
- loci kg query -h
- loci mcp status --help
</verification>

<success_criteria>
- Subcommand-level --help displays help and exits with 0.
- No regression in normal flag parsing.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-refinement/08-02-SUMMARY.md`
</output>
