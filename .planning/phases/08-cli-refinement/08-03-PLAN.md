---
phase: 08-cli-refinement
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified: [scripts/runtime/doctor-loci.mjs, src/interfaces/cli/commands/onboard.ts]
autonomous: true
requirements: [DOCTOR-CRITICAL-TIERS]
must_haves:
  truths:
    - "loci doctor returns exit code 0 if only optional checks fail"
    - "loci onboard reports success if all critical checks pass"
  artifacts:
    - path: "scripts/runtime/doctor-loci.mjs"
      provides: "tiered check logic"
---

<objective>
Refine the health check logic in 'loci doctor' and the 'loci onboard' wizard to distinguish between critical and optional failures. This prevents false-positive "failures" when non-essential tools (like ripgrep or optional cache dirs) are missing but the system remains operational.

Purpose: Accurate system health reporting and smoother onboarding.
Output: Tiered health check system.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/runtime/doctor-loci.mjs
@src/interfaces/cli/commands/onboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier health checks in doctor script</name>
  <files>scripts/runtime/doctor-loci.mjs</files>
  <action>
    Update `scripts/runtime/doctor-loci.mjs`:
    1. Define which checks are `critical` (e.g., node_version, npm_npx, sdk_import, sqlite_backend, config_file).
    2. Define which checks are `optional` (e.g., ripgrep, global_stale_temp).
    3. Update the `check` result structure to include a `critical: boolean` property.
    4. Update the exit logic: Only `process.exit(1)` if `checks.some(c => !c.ok && c.critical)`.
    5. Update the UI to visually distinguish critical vs optional failures (e.g., using a warning symbol for optional).
  </action>
  <verify>
    <automated>node scripts/runtime/doctor-loci.mjs (Ensure it exits with 0 even if ripgrep is missing, but 1 if node is too old)</automated>
  </verify>
  <done>Doctor handles tiered failures correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Refine onboarding wizard reporting</name>
  <files>src/interfaces/cli/commands/onboard.ts</files>
  <action>
    Update `src/interfaces/cli/commands/onboard.ts`:
    1. Update `runDoctor()` to reflect the new tiered logic.
    2. If doctor returns success (0) but had warnings (non-critical failures), display a success message with a "Warnings found" note instead of a "Checks failed" warning.
    3. Ensure the final summary reflects that the system is "Ready" if all critical checks passed.
  </action>
  <verify>
    <automated>loci onboard (Run through the flow and verify final status)</automated>
  </verify>
  <done>Onboarding reporting is accurate and less alarmist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Doctor -> System | Doctor probes system state (files, binaries, versions). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | doctor-loci.mjs | mitigate | Ensure doctor doesn't leak sensitive paths or env vars in its report unless explicitly requested via --json. |
</threat_model>

<verification>
Simulate a missing optional dependency (e.g., rename 'rg' binary temporarily) and verify that doctor and onboard still report success but with warnings.
</verification>

<success_criteria>
- Only critical failures block onboarding or cause non-zero exit codes.
- Users clearly see which failures are essential and which are optional.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-refinement/08-03-SUMMARY.md`
</output>
