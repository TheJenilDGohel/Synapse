---
phase: 08-cli-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/interfaces/cli/ansi.ts]
autonomous: true
requirements: [CLI-STYLE-CHAINING]
must_haves:
  truths:
    - "c.red.bold('text') produces red and bold ANSI output without crashing"
    - "isColorEnabled() still respects NO_COLOR and TTY detection"
  artifacts:
    - path: "src/interfaces/cli/ansi.ts"
      provides: "ANSI styling proxy"
  key_links:
    - from: "scripts/runtime/upgrade-synapse.mjs"
      to: "src/interfaces/cli/ansi.ts"
      via: "c.red.bold usage"
---

<objective>
Refactor the ANSI styling utility to support chaining (e.g., c.red.bold('text')) using a JavaScript Proxy. This resolves Issue #99 where 'synapse upgrade' crashes because it expects c.red to be an object with a bold property.

Purpose: Robust styling API that mirrors common libraries like Chalk while maintaining zero dependencies.
Output: Updated src/interfaces/cli/ansi.ts with Proxy implementation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/interfaces/cli/ansi.ts
@scripts/runtime/upgrade-synapse.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Proxy-based style chaining</name>
  <files>src/interfaces/cli/ansi.ts</files>
  <action>
    Refactor the `c` object in `src/interfaces/cli/ansi.ts`. 
    1. Define a helper that wraps a string in multiple ANSI codes.
    2. Create a Proxy for `c` that traps property access.
    3. Each property access (e.g., `.red`) should return a function that is also a Proxy, allowing further chaining.
    4. When the final function is called with a string, it applies all collected codes.
    5. Ensure `B` (symbols) remains accessible on `c` without styling attempts.
    
    Example Proxy logic:
    - Keep track of an array of codes.
    - Getter returns a new Proxy with the new code added to the array.
    - The Proxy itself is a function that, when called, applies the codes.
  </action>
  <verify>
    <automated>node -e "const { c } = require('./src/interfaces/cli/ansi.js'); console.log(c.red.bold('Test'))" (Ensure it doesn't crash and output contains \x1b[31m and \x1b[1m)</automated>
  </verify>
  <done>c.red.bold('test') works as expected.</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing callers</name>
  <files>scripts/runtime/upgrade-synapse.mjs, scripts/memory/capture-outcome-synapse.mjs, scripts/memory/task-context-synapse.mjs</files>
  <action>
    Verify that the callers mentioned in Issue #99 now work without modification.
    Since we are using a Proxy on the existing `c` object, no changes should be required in these files.
    Run `node scripts/runtime/upgrade-synapse.mjs --help` to confirm it no longer crashes (if it was crashing there).
  </action>
  <verify>
    <automated>node scripts/runtime/upgrade-synapse.mjs --help</automated>
  </verify>
  <done>Callers execute successfully using chained styles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI -> Terminal | Untrusted input (from files/env) is escaped and colored for terminal display. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | ansi.ts | mitigate | Ensure ANSI codes are strictly numeric and terminated to prevent terminal injection. |
</threat_model>

<verification>
Run a small test script to verify multiple combinations:
- c.red('text')
- c.bold('text')
- c.red.bold('text')
- c.bold.red('text')
- c.red.bold.italic('text')
</verification>

<success_criteria>
- Style chaining works for any combination of exported styles.
- Zero new runtime dependencies.
- No regressions in basic styling or NO_COLOR support.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-refinement/08-01-SUMMARY.md`
</output>
