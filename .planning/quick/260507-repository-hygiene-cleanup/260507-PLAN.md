# Quick Task 260507: Repository Hygiene Cleanup - Plan

## Goal

Remove generated local artifacts and polish scaffolded docs metadata so the repository presents as a clean open-source codebase.

## Tasks

1. Remove generated screenshots, local docs build output, transient logs, and local agent scratch data from the working tree.
2. Update ignore rules so the same generated artifacts stay out of source control.
3. Replace scaffolded docs project copy with Synapse-specific wording.

## Verification

- `git status --short` should no longer show transient screenshots, build logs, `.dart_tool`, `docs/build`, or `docs/.agents`.
- `npm run check` should pass.
