import 'package:jaspr/dom.dart';

// --- Design Tokens (Synaptic Deep) ---
// These are now handled in styles.css but kept here for programmatic use if needed.

const primaryColor = Color('var(--primary-color)');
const secondaryColor = Color('var(--secondary-color)');
const backgroundColor = Color('var(--background-color)');
const surfaceColor = Color('var(--surface-color)');
const borderColor = Color('var(--border-color)');
const textColor = Color('var(--text-color)');
const textMutedColor = Color('var(--text-muted)');
